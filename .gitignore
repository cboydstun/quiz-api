node_modules/
.next/
dist/
out/
build/
.turbo/
coverage/

.env
.env*.local
!.env.example

*.tsbuildinfo

# Regenerated by `next dev` / `next build`, and it flips between
# .next/dev/types and .next/types depending on which ran last. Tracking it
# means a dirty tree every time you switch. Nothing needs it committed:
# typecheck passes from a clean clone without it.
next-env.d.ts
.DS_Store
.vercel

# Written by `vercel env pull`. `.env.example` is already tracked, so the
# broad glob does not untrack it.
.env*

# Personal scratch file.
IDEAS.md
