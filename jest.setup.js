// jest.setup.js

// Increase the timeout for all tests
jest.setTimeout(45000); // 30 seconds

// If you need to increase timeout for a specific test file or describe block,
// you can do so in the individual test files like this:
// jest.setTimeout(60000); // 60 seconds